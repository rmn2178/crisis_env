# Data Directory

This directory is reserved for static assets, models, or cached generic data used by the server. 
It is currently not needed by the baseline implementation, but provided here in case teams need to ship static assets alongside their environment.
